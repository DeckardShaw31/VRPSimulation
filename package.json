{
  "name": "vrp-simulation",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node ./tools/static-server.js",
  "run-scenario": "node ./tools/run_scenario.js",
  "batch-run": "node ./tools/batch_run.js",
    "test": "jest --runInBand",
    "analyze": "python analysis/aggregate.py"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "puppeteer": "^20.0.0",
    "express": "^4.18.2"
  }
}
